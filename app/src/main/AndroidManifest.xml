<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cat.copernic.prodis.lacantinadeprodis">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LaCantinaDeProdis">
        <activity
            android:name="cat.copernic.prodis.lacantinadeprodis.MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ComandesActivity" android:label="ComandesActivity" />
        <activity android:name=".CaixerActivity" android:label="ComandesActivity" />
        <activity android:name=".AdministradorActivity" android:label="ComandesActivity" />

    </application>
</manifest>